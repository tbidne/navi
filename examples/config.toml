poll-interval = 10

[battery-state]
repeat-events = false
error-events = "no-repeats"

[[battery-state.alert]]
level = 50
urgency = "low"

[[battery-state.alert]]
level = 30
urgency = "normal"

[[battery-state.alert]]
level = 20
urgency = "critical"

[[battery-state.alert]]
level = 10
urgency = "critical"

[[battery-state.alert]]
level = 5
urgency = "critical"

# Send alert when the current minute is even
[[single]]
command = """
  min=`date +%M`;
  if [[ \"$min % 2\" -eq 0 ]]; then
    echo -n "true"
  else
    echo -n "false"
fi
"""
trigger = "true"

[single.note]
summary = "Even/Odd"
body = "We're even, yay!"
timeout = "10"

